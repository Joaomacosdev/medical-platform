# SMTP (required for notification-service emails)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=no-reply@medical.com

# JWT (optional, dev defaults provided)
JWT_SECRET=chave-local-apenas-para-desenvolvimento

# RabbitMQ (optional, dev defaults provided)
RABBITMQ_USER=notification
RABBITMQ_PASSWORD=guest